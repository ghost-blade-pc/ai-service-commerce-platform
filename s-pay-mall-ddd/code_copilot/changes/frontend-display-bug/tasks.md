# 任务拆分 — 前端输出展示bug
> DDD 推荐顺序：契约/API → 领域模型与端口 → 基础设施适配 → 领域服务编排 → 触发入口 → 配置与验证
> 本次为纯前端变更，不涉及后端 DDD 分层，按页面维度拆分。

## 前置条件

- [ ] 已读取 `code_copilot/rules/*.md`
- [ ] 已确认不涉及资金、退款、订单状态、拼团营销交互
- [ ] 已确认本地分支不是禁止变更分支

## Task 1: 修复单独购买支付确认弹窗价格 ✅

- **目标**: 单独购买点击后支付确认弹窗展示原价而非拼团优惠价
- **DDD 层级**: 不适用（纯前端 HTML 页面）
- **涉及文件**:
    - `docs/dev-ops/nginx/html/index.html` — 修改第 257 行，将 `showPaymentConfirm(goods.payPrice)` 改为 `showPaymentConfirm(price)`
- **关键变更**:
  ```javascript
  // 修改前（第 257 行）
  showPaymentConfirm(goods.payPrice);

  // 修改后
  showPaymentConfirm(price);
  ```
- **依赖**: 无
- **风险标记**: 无
- **验收标准**:
  1. 商品详情页底部"单独购买"按钮显示原价（如 ¥100）
  2. 点击"单独购买"后支付确认弹窗展示的价格与按钮标注一致（也是 ¥100）
  3. 点击"开团购买"/"参与拼团"后支付确认弹窗仍展示拼团优惠价（如 ¥60），不受影响
- **验证命令**: 手动在浏览器中验证上述 3 个场景
- **完成** ✅

## Task 2: 优化退单按钮布局位置 ✅

- **目标**: 将退单按钮从订单卡片右侧独立列移到订单信息区域内部，使布局更紧凑自然
- **DDD 层级**: 不适用（纯前端 HTML/CSS）
- **涉及文件**:
    - `docs/dev-ops/nginx/html/order-list.html` — 调整 HTML 结构、CSS 样式和 JS 渲染逻辑
- **关键变更**:
  1. `.order-item` 布局从 `grid-template-columns: 1fr auto` 改为 `flex` 纵向排列
  2. `.actions` 容器从订单卡片顶层移入 `.order-main` 内部（`.meta` 下方）
  3. 退单按钮样式从固定宽度改为弹性 padding，左对齐
  4. 清理了 CSS 重复定义，合并 `.refund-btn` 与 `.load-more` 共享属性
  5. 简化了 `@media` 查询（移除已不存在的 grid 覆盖）
- **依赖**: 无
- **风险标记**: 无
- **验收标准**:
  1. 订单列表页每张订单卡片的退单按钮显示在订单信息（金额、状态行）下方
  2. 退单按钮左对齐，与订单信息内容对齐
  3. 点击退单按钮功能正常：弹出确认框，确认后退单成功，按钮消失
  4. 移动端（宽度 < 640px）布局正常
  5. 无可退单条件的订单不显示退单按钮（保持原有逻辑）
- **验证命令**: 手动在浏览器中验证上述场景
- **完成** ✅

## 变更摘要
> /apply 全部完成后填写

- **总文件数**: 2 个文件
- **Spec-Plan 偏差记录**: 无
- **遗留问题**: 无
